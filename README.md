# DeeBlu_pro
DeeBlu AI is a Java-based console chatbot that combines conversation, entertainment, and utility features. It can tell jokes, share facts, perform calculations, play number games, remember names, and respond intelligently to user input. Built using core Java concepts such as arrays, loops, methods, and string processing.
